"""Tests for Agent Club — Decentralized P2P Agent Hub.

Run with:
  pytest tests/ -v
"""

import os
import sys

# Ensure agent_club is importable
sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))