# ─── Agent Club Docker Environment ──────────────────
# Copy to .env and customize

# Agent identity (leave blank to generate new)
AGENT_NAME=agent-club

# Ports
AGENT_PORT=8765
DHT_PORT=6881

# Tor ports (only if using tor profile)
TOR_SOCKS_PORT=9050
TOR_CONTROL_PORT=9051
TOR_CONTROL_PASSWORD=agentclub